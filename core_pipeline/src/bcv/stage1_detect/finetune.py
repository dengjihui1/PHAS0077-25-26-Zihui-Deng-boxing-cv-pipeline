"""`bcv-detector-finetune` — train Backend B (2-class red/blue detector).

Stub for now: the labelled detection dataset + warm-start weights are not on this
machine. See ``scripts/recover_melik_detector.md`` for the recovery + training steps.
"""

from __future__ import annotations


def main() -> None:
    raise SystemExit(
        "Backend B fine-tune not implemented yet — the labelled_data dataset and "
        "train15 warm-start weights are not on this machine. "
        "See scripts/recover_melik_detector.md to recover them, then implement this."
    )


if __name__ == "__main__":
    main()
